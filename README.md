# MIPS_FPGA_FUNCIONAL_FINAL
3. **Adicione o SDC**: Assignments → Settings → TimeQuest → aponte para
   `DATAPATHGERAL.sdc`. *(Sem isso o Quartus assume 1 GHz e o timing fica
   sem sentido — foi exatamente esse o problema.)*
